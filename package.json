{
  "name": "webwhisper",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy --only hosting"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.0",
    "lucide": "^0.469.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
